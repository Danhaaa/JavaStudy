
public class C05_Static {
/*
 ### static (정적 영역, 클래스 영역 <=> 인스턴스 영역)

- 같은 클래스의 모든 인스턴스들이 함께 사용하는 공동 영역을
  정적 영역(클래스 영역)이라고 한다
- 앞에 static이 붙은 자원들은 모든 인스턴스가 함께 사용하는 공동 자원이 된다
- static 영역의 자원은 아무리 많은 인스턴스가 있어도 클래스 당 하나이기
때문에 클래스 이름에 .을 찍고 사용할 수 있다
예) Math.pow(), Math.random()
 
 */
	
	
}
